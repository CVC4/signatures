; sorts :
(declare Bool sort)
(declare String sort)
(declare Int sort)

;Arith:
(declare c_int (! v mpz term))
(define int_zero (c_int 0))

; Strings
(declare emptystr term)
(declare char (! v mpz term))
(declare f_str.len term)
(define str.len (# x term (apply f_str.len x)))
(declare f_str.concat term)
(define str.concat (# x term (# y term (apply (apply f_str.concat x) y))))
;(define tail 

;(str.concat x (str.concat y z))
;(apply (apply f_str.concat x) (apply (apply f_str.concat y) z))

